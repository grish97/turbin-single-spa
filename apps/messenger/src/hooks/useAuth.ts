export const useAuth = () => {
  // select user from store
  const user = {};

  const refreshToken = async () => {};

  const setAuth = () => {};

  return {
    user,
    refreshToken,
    setAuth,
  };
};
